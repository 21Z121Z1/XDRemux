name: OPPO native Portrait C prototype v2 (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Superseded by v3 after fixing splice end-marker ownership."
