name: Rust HDR core

on:
  push:
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "crates/xdremux-hdr/**"
      - "Sources/XDRemuxCore/HDR/**"
      - "Tests/XDRemuxCoreTests/HDRRustConformanceOracleTests.swift"
      - "Tests/fixtures/hdr_edr_cases.tsv"
      - "scripts/check_hdr_edr_conformance.sh"
      - "scripts/check_hdr_gainmap_conformance.sh"
      - ".github/workflows/rust-hdr-core.yml"
  pull_request:
    paths:
      - "Cargo.toml"
      - "Cargo.lock"
      - "crates/xdremux-hdr/**"
      - "Sources/XDRemuxCore/HDR/**"
      - "Tests/XDRemuxCoreTests/HDRRustConformanceOracleTests.swift"
      - "Tests/fixtures/hdr_edr_cases.tsv"
      - "scripts/check_hdr_edr_conformance.sh"
      - "scripts/check_hdr_gainmap_conformance.sh"
      - ".github/workflows/rust-hdr-core.yml"

permissions:
  contents: read

jobs:
  hdr-core:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: rustfmt, clippy

      - name: Check formatting
        run: cargo fmt --all --check

      - name: Lint xdremux-hdr
        run: cargo clippy --locked -p xdremux-hdr --all-targets -- -D warnings

      - name: Test xdremux-hdr
        run: cargo test --locked -p xdremux-hdr

      - name: Exercise shared EDR vectors
        run: cargo run --quiet --locked -p xdremux-hdr --bin xdremux-hdr-vectors -- Tests/fixtures/hdr_edr_cases.tsv >/dev/null

      - name: Exercise gain map vectors
        run: cargo run --quiet --locked -p xdremux-hdr --bin xdremux-gainmap-vectors -- Tests/fixtures/hdr_edr_cases.tsv >/dev/null

      - name: Verify repository completion gate
        shell: bash
        run: |
          cat > "$RUNNER_TEMP/xdremux-hdr-core-gate.json" <<'JSON'
          {
            "schema_version": 1,
            "scope": "Add isolated Rust HDR EDR and gain-map reconstruction semantics with precision and raster regressions",
            "checks": [
              {
                "name": "rust-hdr-regressions",
                "kind": "regression",
                "command": ["cargo", "test", "--locked", "-p", "xdremux-hdr"],
                "timeout_seconds": 300
              },
              {
                "name": "rust-edr-vector-integration",
                "kind": "integration",
                "command": ["cargo", "run", "--quiet", "--locked", "-p", "xdremux-hdr", "--bin", "xdremux-hdr-vectors", "--", "Tests/fixtures/hdr_edr_cases.tsv"],
                "timeout_seconds": 300
              },
              {
                "name": "rust-gainmap-vector-integration",
                "kind": "integration",
                "command": ["cargo", "run", "--quiet", "--locked", "-p", "xdremux-hdr", "--bin", "xdremux-gainmap-vectors", "--", "Tests/fixtures/hdr_edr_cases.tsv"],
                "timeout_seconds": 300
              },
              {
                "name": "rust-hdr-clippy",
                "kind": "static",
                "command": ["cargo", "clippy", "--locked", "-p", "xdremux-hdr", "--all-targets", "--", "-D", "warnings"],
                "timeout_seconds": 300
              }
            ]
          }
          JSON
          python3 scripts/agent_completion_gate.py run \
            --base origin/main \
            --plan "$RUNNER_TEMP/xdremux-hdr-core-gate.json"
          python3 scripts/agent_completion_gate.py verify \
            ".codex/verification-receipts/$(git rev-parse HEAD).json"

  hdr-conformance:
    runs-on: macos-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          toolchain: stable
          components: rustfmt, clippy

      - name: Pin Swift HDR branch contract
        run: swift test --filter HDRRustConformanceOracleTests/testEDRSharedVectorsCoverPrecisionAndBranchBoundaries

      - name: Pin Swift gain map contract
        run: swift test --filter HDRRustConformanceOracleTests/testGainMapSharedVectorsExhaustByteDomainAndPadding

      - name: Compare Swift and Rust EDR semantics
        run: bash scripts/check_hdr_edr_conformance.sh

      - name: Compare Swift and Rust gain map semantics
        run: bash scripts/check_hdr_gainmap_conformance.sh

      - name: Verify cross-language completion gate
        shell: bash
        run: |
          cat > "$RUNNER_TEMP/xdremux-hdr-conformance-gate.json" <<'JSON'
          {
            "schema_version": 1,
            "scope": "Prove Rust EDR, knee, and gain-map reconstruction match current Swift precision and raster semantics",
            "checks": [
              {
                "name": "swift-hdr-branch-contract",
                "kind": "regression",
                "command": ["swift", "test", "--filter", "HDRRustConformanceOracleTests/testEDRSharedVectorsCoverPrecisionAndBranchBoundaries"],
                "timeout_seconds": 600
              },
              {
                "name": "swift-gainmap-contract",
                "kind": "regression",
                "command": ["swift", "test", "--filter", "HDRRustConformanceOracleTests/testGainMapSharedVectorsExhaustByteDomainAndPadding"],
                "timeout_seconds": 600
              },
              {
                "name": "swift-rust-edr-conformance",
                "kind": "integration",
                "command": ["bash", "scripts/check_hdr_edr_conformance.sh"],
                "timeout_seconds": 1200
              },
              {
                "name": "swift-rust-gainmap-conformance",
                "kind": "integration",
                "command": ["bash", "scripts/check_hdr_gainmap_conformance.sh"],
                "timeout_seconds": 1200
              }
            ]
          }
          JSON
          python3 scripts/agent_completion_gate.py run \
            --base origin/main \
            --plan "$RUNNER_TEMP/xdremux-hdr-conformance-gate.json"
          python3 scripts/agent_completion_gate.py verify \
            ".codex/verification-receipts/$(git rev-parse HEAD).json"
