name: PS3 semantic 12-role runner trigger
on:
  push:
    branches: [research/ps3-semantic-12role-runner]
permissions:
  contents: read
jobs:
  trigger-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo trigger
